@model Producto

@{
    ViewData["Title"] = "Detalles Producto";
}

<h1>@ViewData["Title"]</h1>
<p>En esta página se muestran los detalles del producto.</p>

<form>
    <div class="mb-3">
        <label class="form-label">Nombre</label>
        <input asp-for="nombre" class="form-control " readonly />
    </div>
    <div class="mb-3">
        <label class="form-label">Descripción</label>
        <input asp-for="descripcion" class="form-control" readonly>
    </div>
    <div class="mb-3">
        <label class="form-label">Cantidad</label>
        <input asp-for="cantidad" class="form-control" readonly>
    </div>
    <div class="row">
        <div class="col-6">
            <button type="button" class="btn btn-success" onclick="location.href='@Url.Action("Index","Producto")'">Volver</button>
        </div>
    </div>
</form>

